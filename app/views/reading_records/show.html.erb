<h1>読書記録詳細</h1>

<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= @reading_record.title %></h5>
    <p class="card-text">
      <strong>著者:</strong> <%= @reading_record.author %><br>
      <strong>読了日:</strong> <%= @reading_record.finished_on.strftime('%Y年%m月%d日') %><br>
      <strong>読者:</strong> <%= @reading_record.user.name %>
    </p>
  </div>
</div>

<div class="mt-3">
  <%= link_to '編集', edit_reading_record_path(@reading_record), class: 'btn btn-warning' %>
  <%= link_to '一覧に戻る', reading_records_path, class: 'btn btn-secondary' %>
  <%= link_to '削除', @reading_record, method: :delete, data: { confirm: '本当に削除しますか？' }, class: 'btn btn-danger' %>
</div>
