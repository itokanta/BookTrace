<%= form_with(model: reading_record, local: true) do |form| %>
  <% if reading_record.errors.any? %>
    <div class="alert alert-danger">
      <h2>保存に失敗しました。下記をご確認ください。</h2>
      <ul>
        <% reading_record.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :title, 'タイトル' %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :author, '著者' %>
    <%= form.text_field :author, class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :finished_on, '読了日' %>
    <%= form.date_field :finished_on, class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <div class="form-check">
      <%= form.check_box :public, class: 'form-check-input' %>
      <%= form.label :public, '公開する', class: 'form-check-label' %>
    </div>
    <small class="form-text text-muted">
      チェックを入れると、他のユーザーにも読書記録が表示されます。
    </small>
  </div>

  <div class="form-group">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %> 